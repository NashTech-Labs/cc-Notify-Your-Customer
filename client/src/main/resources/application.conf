
client_db = {
  dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
  initializationFailFast = no
  properties = {
    databaseName = "ping_client"
    user = "postgres"
    password = "postgres"
    serverName = "localhost"
  }
  numThreads = 10
}
