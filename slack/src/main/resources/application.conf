//**************************************
// Client api configuration
//**************************************
client {
  api {
    host = "http://localhost:9001"
    host = ${?CLIENT_API_HOST}
    url = "/v1/config/slack/"
    url = ${?PING_API_URL}
    access {
      token = "abcde"
      token = ${?PING_API_TOKEN}
    }
  }
}
